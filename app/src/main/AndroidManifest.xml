<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Söz: sıfır izin. INTERNET dahil hiçbir izin istenmiyor. -->
    <!-- Sonuç paylaşımı da izinsizdir: kart önbelleğe yazılır, sistemin paylaşım
         sayfasına yalnızca o tek dosya için okuma yetkisi verilir (FileProvider). -->

    <!-- androidx.core (1.9+) kendi kütüphane manifestinden uygulamanın paket adıyla
         imza düzeyinde bir izin tanımlar ve kullanır: ContextCompat.registerReceiver
         RECEIVER_NOT_EXPORTED ile çağrılınca API 33 altında alıcıyı onunla korur.
         Reyon hiç yayın alıcısı kaydetmiyor; v1.0.0'ın dex'inde registerReceiver de
         iznin adı da hiç geçmiyordu (R8 o yolu budamıştı). İzin ölü ağırlıktı ama
         manifestte durdukça yukarıdaki sözü bozuyordu; tools/apk_dogrula.py artık
         Release'ten önce bunu yakalıyor. Biri ileride registerReceiver(…,
         RECEIVER_NOT_EXPORTED) eklerse bu iki satır kaldırılmalı, yoksa API 26–32'de
         "Permission … is required" ile çöker. -->
    <permission
        android:name="${applicationId}.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION"
        tools:node="remove" />
    <uses-permission
        android:name="${applicationId}.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION"
        tools:node="remove" />

    <application
        android:allowBackup="true"
        android:appCategory="productivity"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Reyon">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            tools:ignore="DiscouragedApi,LockedOrientationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.share"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/share_paths" />
        </provider>
    </application>

</manifest>
